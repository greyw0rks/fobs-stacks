# DATABASE_URL — Neon Postgres connection string for the early-access waitlist.
# Get it from the Neon console (or `neonctl connection-string`). Pooled or
# direct both work; the app talks to Neon over HTTP.
#   postgresql://<user>:<password>@<endpoint>.neon.tech/<db>?sslmode=require
DATABASE_URL=
